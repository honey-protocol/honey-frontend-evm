export enum LoanWorkFlowType {
  none,
  depositNFT,
  loanOrBorrow,
}

export enum LendWorkFlowType {
  none,
  lendOrWithdraw,
}

export enum LiquidationWorkFlowType {
  none,
  collectionBid,
  collateralBid,
}
